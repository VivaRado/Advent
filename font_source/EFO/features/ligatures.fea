# Ligatures Start;
feature liga {
	sub f l by fl;
	sub f i by fi;
	sub f f l by ffl;
	sub f t by ft;
	#
	sub t t by t_t;
	sub w w w by w_w_w;
	#
	sub gamma gamma by gamma_gamma;
	sub gamma kappa by gamma_kappa;
	sub lambda lambda by lambda_lambda;
	#sub sigma tau by sigma_tau;
	#
} liga;
# Ligatures End;
